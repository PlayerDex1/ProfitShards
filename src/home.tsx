import React from 'react';
import ActivityStream from './ActivityStream';

const Home = () => {
    return (
        <div>
            <h1>Home</h1>
            <ActivityStream />
        </div>
    );
};

export default Home;